const a = 5;

// El eslint sirve para hacer comprobaciones de codigo estatico a medida que se va escribiendo el codigo
// Para automatizar el uso de eslint se usa un script en el package.json -> eslint: ""
function getA() {
  return a;
}
